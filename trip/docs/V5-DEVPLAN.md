> **Historical: v5 shipped and this plan is closed. Kept for reference because
> DECISIONS.md, `playwright.config.ts` and
> `docs/v5-ai-concierge-feasibility.md` still cite it by name.**
>
> **Do not build from this file.** Several of its "verified codebase facts" and
> locked rows have since been overturned: FU-18 and the anon-uid allowlist are
> retired by D-205, and the shipped `firestore.rules` is a capability-token
> model that never reads `request.auth`; `gen-sw.mjs`'s `ROUTE_HTML` was
> replaced by route discovery in S265; PMTiles is a NO-GO per D-197, with
> D-286 the shipped answer; and the Phase-4 push slices (S201/S202) were never
> built. The final version's scope lives in `V-FINAL-DEVPLAN.md`.

# V5-DEVPLAN — M19: the v4→v5 revamp (Travel Mode · Trip Packs · motion platform · first backend)

_Written 2026-07-16 from the approved v5 master plan, with every contested claim verified in-repo. This file is the authoritative milestone source for M19: every slice's goal, DoD, and risk notes live here._

## 0. Charter

v5 turns the app from a trip *planner* into a trip *companion*:

- **Travel Mode** (the centerpiece): a chrome-free, mobile-first `/travel` route with a Now/Next hero card, today's agenda, weather and essentials, big touch targets, fully offline, iPhone 15 Pro / Pro Max first. Enter and exit easily at any trip phase, with a date picker for other days. It is discoverable before the trip, not only during it: a persistent Travel Mode entry button lives in the top-right of the main nav chrome on every page, not just the Home hero button, so the mode can be previewed and explored any time before Dec 9 once it ships. It is not gated behind the trip-date window. A clear exit affordance is a hard requirement, not an optional polish item (decided 2026-07-16).
- **Trip Packs**: the app becomes multi-trip-capable via low-risk parameterization. The current trip's bytes stay untouched, grandfathered forever.
- **Motion/design platform**: View Transitions, scroll-driven CSS and framer springs, all reduced-motion-safe and near-zero bundle cost, plus a "Glass 2.0" visual refresh via a one-slice design-token extraction.
- **First backend**: one free Cloudflare Worker. An AI concierge (`/chat` SSE, Gemini free tier with a Groq fallback) and, runway permitting, a morning-briefing Web Push.
- **Everything free, card-free.** Decided 2026-07-15: the backend is greenlit on free tiers only, Trip Packs is in, and the whole thing ships well before Dec 9 so Travel Mode is field-tested before the flight.

**Calendar**: feature-complete **Oct 15** → deployed + first real-iPhone field test **Nov 9** → freeze-except-fixes → second field test ~**Dec 1** (go/no-go). Cut order under pressure: push, then concierge, then Trip Packs UI, then trim the Phase-5 sweep. Never cut: Phase 0, Travel Mode core, the field-test window.

**Scope fences (v5 does not include)**: anything paid or card-required · native app · full D-002 inversion · realtime collab editing · AI itinerary *generation* (the concierge is chat only) · visual rebrand · i18n · **light mode** (Travel Mode gets an outdoor high-legibility toggle instead) · S168 ESLint · FU-2 · Next 16 / Tailwind 4 · flight-status APIs (rejected with evidence; deep-link to the airline or FR24 instead).

## 1. Verified codebase facts (binding for every slice)

1. **`scripts/gen-sw.mjs` `ROUTE_HTML` was hand-maintained when this plan was written; it no longer exists.** The walk() then auto-discovered only `_next/static`, icons and the manifest, the comment cited the S153 `/journal` omission bug, and any new route (`/travel`) had to be added to `ROUTE_HTML` explicitly, with `e2e/pwa.spec.ts` as the tripwire. Recorded as D-170. S265 has since replaced the hand-kept literal with discovery by walking `out/` (`scripts/gen-sw.mjs:609-612`, the discovery branch at `:633-634`), which is exactly D-170's own "Changes if" branch, so the hand-edit obligation in S184 below is gone. `e2e/pwa.spec.ts` no longer pins a precache count either: it derives the route list from `out/` and asserts a generous floor (`pwa.spec.ts:443`).
2. **`app/template.tsx`** is a CSS `.animate-route-fade` shell (S67) remounting per navigation, with a `useReducedMotion()` React-level branch. The VT wrapper has to suppress the route fade when a View Transition drives navigation, or every navigation double-animates. The D-007/D-056 reduced-motion "NONE" hard guarantee must survive.
3. **`viewportFit: 'cover'` is already set** (`app/layout.tsx:88`, S61/D-070). Travel Mode only adds per-component `env(safe-area-inset-*)` padding.
4. **The byte-parity facade pattern already exists**: `lib/trip-data.ts` re-exports the date backbone from `core/dates/` (S93/D-099), and Trip Packs extends that pattern. `core/storage/gateway.ts` is the single key registry (D-097 LOCKED, local+session, never-throw), so `keyFor()` and `activeTripId` land there and nowhere else.
5. **Firestore is already per-trip**: `lib/firebase-config.ts:46` (`NEXT_PUBLIC_TRIP_ID`, default `nepal-japan-2026`). Never migrate the live tree; new packs get new ids.
6. **D-006 is the countdown-target lock (Dec 9 → Jan 9), not "no multi-trip".** Trip Packs *amends* it: dates become the default pack's and the derived constants stay byte-identical. The D-005/D-126 annotations stay intact.
7. **~118 raw `navy-*` usages** across `components/` and `app/` tsx (measure exactly at slice time), and **21 visual baselines** in `e2e/visual.spec.ts-snapshots/` at the time of writing. The one-re-baseline budget is real. (The directory holds 24 as of 2026-08-10: 8 scenarios × desktop/mobile/tablet.)
8. **Playwright was one `chromium` project when this plan was written.** The two iPhone projects (`iphone-15-pro` 393×852 · `iphone-15-pro-max` 430×932, DPR3) were net-new then and have since landed at `playwright.config.ts:97` and `:106`, each `testMatch`-scoped to `e2e/tm-acceptance.spec.ts` alone, which the default `chromium` project (`:76`) `testIgnore`s.
9. **Versions frozen through Dec 9**: next 15.5.20 · react 19.2.7 · framer-motion 12.42.2 (the rename to `motion` was declined; already v12 per D-132) · tailwindcss 3.3.3 · maplibre-gl 5.24.0.
10. **The chrome-suppression surface** is the pathname conditionals in the `app/chrome-islands.tsx` client components (Navbar/Footer/BottomTabBar/QuickAddFab/QuickAddHost/ExpenseLogHost), not a `layout.tsx` restructuring. Guest gating is `components/token-gate.tsx`; `/travel` inherits guests-blocked, and the spec should assert that stance explicitly.
11. **`nextUp()` is pure** (`lib/whats-next.ts`) and is the engine for the hero card's phase state machine. `components/today-panel.tsx` and `components/day-strip.tsx` exist for extraction and reuse. The `?today=` clock sim (D-075, sessionStorage) stays decoupled from Travel Mode's `?date=`; they compose.
12. **FU-18 was the one open P1 when this plan was written; it is now retired.** Live rules then allowed any anonymous user read/write/delete, and a hardened uid-allowlist was drafted in `firestore.rules` for manual deploy by the account owner per D-044. D-205 (2026-07-18) rejected that plan outright rather than tightening it, and replaced the model with capability-token trips: the shipped `firestore.rules` is 31 lines, reads `allow get, list, write: if true` under a known `tripId`, and never reads `request.auth`. Security rests on trip ids being unguessable and unenumerable. The Auth row in section 2, S171, S200 and Phase 4's FU-18 precondition are all superseded with it, and are kept below only as the historical record.

## 2. Locked decisions (recorded as D-164…D-173, see DECISIONS.md)

| Topic | Call |
|---|---|
| Travel Mode shape | Route **`/travel`**, `?date=YYYY-MM-DD` (default `getTodayInTrip()`; pre-trip → Day 1); decoupled from `?today=` (D-075). A `travelMode` gateway flag so a PWA relaunch re-enters. Guests blocked. **Entry point (decided 2026-07-16, amends S190): a persistent Travel Mode button in the top-right of the app-wide nav chrome (`chrome-islands.tsx` surfaces, alongside Navbar/BottomTabBar), available on every page and not gated by the trip-date window.** Pre-trip visitors can enter and explore the pre-trip Day-1 state any time once it ships. The exit affordance (44px X, restores the prior route, no history trap) is a hard DoD requirement for S190, not optional polish. |
| Light mode | No. An outdoor high-legibility toggle inside Travel Mode instead |
| AI concierge | Build. Gemini free tier primary, Groq fallback (llama-3.3-70b 30 RPM/1k RPD · llama-3.1-8b 14.4k RPD); keys Worker-only |
| Push | ~~Build if runway allows, after the concierge proves the Worker. Raw VAPID preferred: it avoids the FCM `firebase-messaging-sw.js` collision with the single generated SW. Protocol finalized in the S197 spike~~ **Never built. D-167 (the morning-briefing GO) and D-175 (the raw-VAPID protocol GO) stand on paper only: there is no `push`/`notificationclick` handler in `scripts/gen-sw.mjs` and no `pushManager` call anywhere in the app. S201 and S202 below were not executed.** |
| Auth | Full #15 rejected. FU-18 anon-allowlist now, plus a small Google sign-in slice before departure (stable uids, free on Spark) |
| Flight status | Rejected. No card-free API exists (FlightAware/AeroDataBox/aviationstack/OpenSky all disqualified), so deep-links instead |
| View Transitions | Manual `document.startViewTransition()` progressive enhancement (a `useViewTransition()` helper). No React canary or experimental flag. template.tsx reconciled per fact 2 |
| Trip Packs storage | **Grandfather, never migrate** (LOCKED). The current trip keeps its legacy keys byte-identical; future trips use `trip:{id}:{slot}` via one `keyFor()`. Switching writes `activeTripId` and does a full reload. The switcher stays hidden until a second pack exists, and the live Firestore tree is never migrated |
| PMTiles | ~~Spike-gated stretch: opt-in OPFS download, never SW-precached. The GitHub Pages 100MB cap makes the extract-sizing spike the go/no-go~~ **Superseded: the S209 spike ran, and D-197 (2026-07-17) ruled S210 NO-GO for v5 and dropped it from the candidate pool. The shipped answer is D-286 — the map engine is precached, tiles are not.** |
| D-002 | Unchanged and reaffirmed. localStorage stays the source of truth; Trip Packs is key namespacing |

## 3. Phase plan and slice ledger (S171–S213; S162–S166 stay reserved-dropped and are never reused)

Dependency shape:

```
P0 (security+debt) ──→ P1a (motion/design) ──→ P2 (Travel Mode) ──→ P5 (sweep+ship)
        │────────────→ P1b (Trip Packs a) ──→ P3 (Packs done) ──↗
        └─(FU-18 before pushSubs)→ P4 (backend; spike may run ∥ P2) ──↗
P1a ∥ P1b in worktrees · P2 does not block on P1b (escape hatch: current schema)
```

### Phase 0 — Security + debt (4–5 slices)

| Slice | Goal / DoD | Hardness |
|---|---|---|
| **S171** | **FU-18 runbook and owner hand-off.** Verify the drafted allowlist at `firestore.rules:36-43` still matches the live schema (including any future `pushSubs` extension point, commented), then write the owner action list: the exact deploy steps (manual per D-044), how each friend reads their anon uid, and the localStorage fixture-capture snippet below. DoD: doc committed, owner notified, no rules deployed automatically. | Trivial. Docs and coordination only, nothing to verify |
| **S172** | **Migration fixture + baseline tag.** Land the fixture harness: a `e2e/fixtures/live-v5-vault/` README plus a loader util that seeds a real captured dump via `addInitScript`. The dump itself comes from the owner using S171's snippet; a placeholder synthetic dump keeps specs green until then and is clearly marked as not acceptance evidence. Tag `v4-visual-baseline` on the pre-v5 main commit. DoD: loader and README committed, tag pushed. | Easy. Contained fixture and tooling, no existing code touched |
| **S173** | **FU-39.** Command-palette close-X ≥44px. Add its visual baseline first, then fix; axe and visual green. | Easy. Small, well-scoped a11y fix; the only wrinkle is the baseline-then-fix sequencing |
| **S174** | **FU-37.** Expenses backup-restore surface. The Vault is itinerary-only per D-098, so expenses get an export/restore path consistent with D-156 tombstone-replace. DoD: restore-under-sync spec green. | Moderate. A new merge/sync-correctness surface on a second domain, but mechanical relative to the landed S145 pattern |
| **S175** | **FU-6.** The PageHero-on-Home question goes to the owner; either answer closes it (mount `<PageHero variant="plan" as="h2">` or record it declined). Zero code until answered. | N/A. Zero-code decision, blocked on the owner |

### Phase 1 — Platform foundations (8 slices; tracks (a) and (b) run in parallel worktrees)

| Slice | Goal / DoD | Hardness |
|---|---|---|
| **S176** | **Phase-1 blueprint.** The `TripConfig{id,label,start,end,legs[],contentRef}` shape, where `legs[]` generalizes the binary nepal/japan union. That generalization is the hard part: budget NPR/JPY (D-110), expenses, recap and the content routes all enumerate it. Also the VT/template.tsx reconciliation design honoring fact 2, and the final wording of D-164…D-173. No production code. | Hard. A novel-shape blueprint finalizing 10 LOCKED decisions; no code, but high judgment risk |
| **S177** | (a) **Design-token extraction.** Retokenize the ~118 `navy-*` usages into semantic CSS vars in one slice, spending the one budgeted re-baseline of the 21 snapshots. Later slices change tokens, not components. | Hard. Widescale mechanical retokenization with no re-baseline budget left over if it is missed |
| **S178** | (a) **Glass 2.0.** Subtler blur, 1px gradient borders, existing noise on glass. Token-level changes only, and no re-baseline beyond the declared design freeze. | Moderate. A cross-cutting visual pass riding S177's tokens: contained, but it touches every glass surface |
| **S179** | (a) **VT wrapper.** A `useViewTransition()` helper (manual `document.startViewTransition`, `@supports`/feature-detected, no-op fallback). template.tsx's route fade is suppressed when VT drives navigation, and reduced-motion = NONE is preserved (D-007/D-056). `e2e/motion.spec.ts` extended. | Hard. A novel pattern with no precedent in this codebase, and it has to reconcile with template.tsx without double-animating |
| **S180** | (a) **Scroll-driven CSS conversions.** `route-accent-engine.tsx` first, `@supports`-guarded, with a Firefox static fallback and a reduced-motion audit. | Moderate. New pattern but scoped to one component, with an established reduced-motion contract to follow |
| **S181** | (b) **`core/trips/` + `core/dates` parameterization.** The facade keeps exporting the same constant names, derived from the default pack. Byte-parity is gated on the existing FU-10/S82 suites staying unchanged: ~25 unit-test files untouched, and that constraint goes into the slice verbatim. | Very hard. It sits on the date backbone every countdown and calendar calculation depends on; any drift is a silent app-wide regression |
| **S182** | (b) **`core/content` parameterization.** Content moves behind `contentRef`; the nepal/japan routes read through it; zero visual drift. | Moderate. Same shape as S181, but display data rather than date math other correctness depends on. Lower blast radius |
| **S183** | (b) **Gateway `keyFor()` + `activeTripId`.** In `core/storage/gateway.ts` only. Legacy keys are returned verbatim for the default pack (grandfathering), per-key `Store` facts are preserved (`tripPlannerTodayOverride` stays sessionStorage), and the gateway unit suite is extended. | Hard. It touches the single LOCKED (D-097) key registry that every persisted feature depends on |

_S181 and S183 are the two riskiest Phase-0/1 slices even though neither is a new subsystem. Both sit directly on LOCKED invariants (the byte-parity test suite; D-097) with app-wide blast radius if wrong, so verify those two especially strictly on a real run. S176 is the one true blueprint slice in this range, and there is no server-side work anywhere in Phase 0 or 1, as expected._

### Phase 2 — Travel Mode (9 slices, the centerpiece)

| Slice | Goal / DoD | Hardness |
|---|---|---|
| **S184** | **`/travel` route bundle**: `app/travel/page.tsx` (lazy islands per the D-131 pattern), chrome suppression via pathname conditionals in the `chrome-islands` components, **`travel/index.html` added to `ROUTE_HTML`**, an explicit guest stance (blocked, and the spec asserts it), and safe-area padding. DoD: the route serves, is chrome-free and precached (sw.js count bumps), and `pwa.spec` plus `guest-route-gate.spec` are green. Carries the four iPhone-15-Pro hardening items from section 6 (safe-area fallback, thumb-zone pin, overscroll mitigation, Dynamic Island comment). | Hard. The first Travel Mode slice: a mandatory `gen-sw.mjs` hand-edit (miss it and precache silently breaks), chrome suppression across 6 islands, and it sets the precedent for the rest of Phase 2 |
| **S185** | **Now/Next hero card.** A phase state machine (upcoming→now→done) on pure `nextUp()`, Flighty-inspired, with a framer spring flip inside LazyMotion strict. Carries the three Flighty refinements from section 6 (progress bar, tap-to-expand, manual recalculate). | Very hard. The phase's one genuinely novel interaction pattern, with no existing precedent in the codebase, and now with more novel sub-states |
| **S186** | **Agenda extraction.** A shared agenda component out of `today-panel.tsx`, parameterized by an optional `date`. Travel Mode's agenda gets 48pt rows, phase styling and preserved done-tracking, while today-panel stays pixel-unchanged so the visual baseline holds. | Hard. An extraction from the the Today screen's core component under a frozen visual baseline; regression risk is the whole point |
| **S187** | **Date picking.** `day-strip.tsx` reuse, a `?date=` param bounded to Dec 9–Jan 9 with an empty state outside it, a non-today "preview" banner, and a pre-trip default of Day 1 plus "Trip starts in N days". Composes with the `?today=` sim. | Moderate. Reuses an established component and pattern; the main risk is correct composition with `?today=` at the trip-window edges |
| **S188** | **Essentials block.** Weather from the existing Open-Meteo cache, leg currency via Frankfurter (SW stale-while-revalidate plus offline cache), safety numbers, and a next-flight deep-link (airline/FR24). | Moderate. A composition of already-shipped pieces plus one new SW caching strategy, contained to one component |
| **S189** | **Outdoor high-legibility toggle.** Travel-Mode-local token overrides for contrast and size, persisted as a gateway flag, axe clean in both states. | Easy. Token-level overrides on the already-landed S177/S178 system, a well-precedented pattern |
| **S190** | **Enter/exit affordances.** The persistent top-right nav-chrome Travel Mode button (decided 2026-07-16) is the primary, always-discoverable entry point, available pre-trip so the mode can be previewed any time before Dec 9, not gated by the trip-date window. Plus the home hero button, the in-trip card, and an arrival auto-suggest toast. The 44pt exit X is a hard DoD requirement, not optional, and restores the prior route with no history trap. The `travelMode` gateway flag makes a PWA relaunch re-enter. | Hard. Router/history correctness (no history trap) across four entry surfaces, with the nav-chrome button now the primary one, plus a novel arrival-detection heuristic. The nav-chrome button has to render correctly in both pre-trip and in-trip states |
| **S191** | **iPhone Playwright projects + Travel Mode acceptance net.** The `iphone-15-pro` and `iphone-15-pro-max` projects (testMatch-scoped), and the TM-1…12 specs from section 4. | Hard. A test-infrastructure slice that the whole phase's acceptance evidence rests on; a gap here undermines everything else in Phase 2 |
| **S192** | **Offline verification + Travel Mode polish.** Full offline including reload, the Dec 18/19 timezone-boundary day pinned, reduced-motion, no overflow on either viewport, and per-state Travel Mode visual baselines. | Hard. Cross-cutting verification across all of S184–S190, plus a genuine timezone-boundary edge case |

_The section-6 scope-note items on S184 and S185 add polish and edge-case surface but do not change either slice's fundamental risk category, so they are folded into the scores above rather than listed separately._

### Phase 3 — Trip Packs completion (4 slices)

| Slice | Goal / DoD | Hardness |
|---|---|---|
| **S193** | **Adoption step** for the 9 non-vault `nepal_japan_*` slots. One-shot, idempotent and non-destructive: adopted legacy keys are never deleted. Verified against the S172 real fixture byte-for-byte, with the D-073 old-build-overwrite loop and the D-096 quarantine path intact. | Very hard. A data migration over real trip data. This project's ledger flags migrations (cf. D-139) as its historically highest-regression-risk category |
| **S194** | **Hidden Settings switcher.** Renders only when more than one pack exists; switching writes `activeTripId` and does a full reload. | Easy. A small conditional-render UI plus one write and a reload, with no merge or sync logic |
| **S195** | **Multi-trip isolation e2e.** A second TRIP_ID never touches live-tree data, namespaced keys do not collide, and the guest/traveler stances hold per pack. | Hard. It verifies the storage/gateway isolation guarantee itself, and a false pass risks silently corrupting the live Firestore tree |
| **S196** | **Hydration mount-gate audit.** The bounded risk when the active pack is not the default one: audit every mount-gated island and spec the reload-switch path. | Hard. Bounded risk but broad surface area (every mount-gated island app-wide), and a missed island is a silent bug elsewhere |

### Phase 4 — Backend activation (6 slices; highest variance. Deployed FU-18 rules are a hard precondition for S201 onward)

| Slice | Goal / DoD | Hardness |
|---|---|---|
| **S197** | **Feasibility spike.** Create keys, paste the exact Gemini RPD from AI Studio into DECISIONS, confirm the Groq numbers, decide the push protocol (raw VAPID favored), and give a per-feature go/no-go. | Moderate. Research and decision output rather than durable code, but these are first-of-kind judgment calls with real downstream lock-in risk |
| **S198** | **`worker/` deployable.** A Cloudflare Worker: `/chat` SSE proxy, Firebase ID-token verify, CORS locked to the Pages origin, Gemini→Groq fallback, secrets in Worker env only. Mocked at the network boundary for the continuous net; the live smoke test stays manual. | Very hard. The first server-side anything in this app's history (there has been none through v1–v4). New subsystem, secret handling, no existing pattern to extend |
| **S199** | **Travel Mode chat panel.** The designed degraded states are the acceptance: offline hides the card, a 429 shows "concierge resting", and no retry loop burns quota. Verification greps the built `out/` for key material. | Hard. The degraded-state logic and the key-material leak-grep discipline are easy to get subtly, silently wrong |
| **S200** | **Google sign-in.** Free on Spark. The allowlist is swapped to 4 stable Google uids, which kills the anon-uid-rotation mid-trip failure mode. The rules redeploy is manual, using the S171 runbook. | Moderate. A well-trodden Firebase Auth pattern, and the risky part (the rules redeploy) is explicitly a manual owner action |
| **S201** | (runway) **Push opt-in.** UI plus PushSubscription → `trips/{tripId}/pushSubs/{uid}` (a rules extension), with the iOS installed-PWA 16.4+ onboarding step documented. | Moderate. A contained client PushSubscription API and UI once S198's Worker contract exists. It touches backend-adjacent rules but writes no Worker code |
| **S202** | (runway) **Pulse cron sender.** Two of the five free Cloudflare cron triggers (one per leg's 6 AM). SW push and notificationclick handlers are merged into the gen-sw output behind a dormant-unless-configured gate (the D-038/D-055 pattern), producing "Day N — first item at HH:MM". Carries the Badging API tie-in from section 6 (`setAppBadge`/`clearAppBadge` from the same handlers). | Hard. The first Cloudflare cron trigger and the first SW push-handler merge into the hand-maintained gen-sw surface. Keep the Worker half and the client Badging tie-in in one slice: splitting them across owners is where the regression risk lives, and the Worker half dominates that risk |

### Phase 5 — Revamp sweep + polish + ship (11 slices; runs last and alone)

| Slice | Goal / DoD | Hardness |
|---|---|---|
| **S203** | Phase-based itinerary cards + glance pill | Trivial. Visual and layout work over already-existing data, no new subsystem |
| **S204** | Countdown ring | Trivial. A presentational wrap around already-correct, already-tested countdown math |
| **S205** | Home bento grid (below the countdown; within the current Home First-Load budget via lazy islands) | Moderate. The layout is easy; the perf budget is the part that bites if done carelessly. Established D-131 lazy-island pattern |
| **S206** | Packing checklist with country templates (gateway-persisted) | Easy. A new `STORAGE_KEYS` entry following the existing gateway CRUD pattern exactly |
| **S207** | Currency converter ⌘K command (Frankfurter, offline cache) | Easy. Reuses an already-wired data source (S188) and an existing command-palette surface |
| **S208** | (stretch) Journal photos and mood ride-alongs on the D-159/D-160 pipeline | Moderate. Additive to an established pipeline, but a hard dependency for S219's capstone later |
| **S209** | (stretch) **PMTiles spike.** Extract sizing against the GitHub Pages 100MB cap; go/no-go | Hard. A novel binary tile format against a hard external size cap. The "stretch"/spike shape does not reduce the real technical uncertainty |
| **S210** | (stretch, S209-gated) PMTiles offline maps: `addProtocol` on maplibre 5.24, opt-in OPFS download, never SW-precached | Very hard. First use of a novel tile format plus the OPFS storage API. A new subsystem gated on S209, with real risk of subtle offline and storage bugs |
| **S211** | Route-by-route motion sweep (VT, scroll-driven, springs; reduced-motion audit) | Hard. A cross-cutting sweep across every route, and the reduced-motion contract has to hold everywhere at once |
| **S212** | Final a11y pass on both nets (axe serious/critical = 0 app-wide, including Travel Mode states) | Hard. Cross-cutting breadth (every route plus every Travel Mode phase state), and it needs judgment to avoid whack-a-mole regressions |
| **S213** | **v5.0.0 bump and ship.** Mirror deploy, scrub (full marker-set grep), live verify (200s, zero leaks, zero key material) | Easy. Direct historical precedent: the S110/M14/M15/M17 deploy passes already executed this exact playbook |

_Phase 4 is the first server-side work in this project's history (there was none through v1–v4), so treat it as first-of-its-kind, higher-uncertainty work rather than a routine addition. S209 and S210 are scored Hard/Very hard deliberately despite being marked "stretch": the underlying engineering risk (binary tile format, OPFS, the 100MB cap) is real regardless of whether the feature ships._

## 4. Acceptance centerpiece

**TM-1…12** (S191/S192): (1) enter and exit at pre-, during- and post-trip via `?today=`, with designed states and no blanks; (2) exit restores the prior route with no history trap; (3) Travel Mode state and the picked date survive reload; (4) the agenda is exact against the seeded vault; (5) the date picker is bounded Dec 9–Jan 9 with an empty state; (6) the Dec 18/19 timezone-boundary day is pinned; (7) Travel Mode edits persist to the vault; (8) 393×852 and 430×932 DPR3 show no overflow and correct safe-area padding; (9) zero chrome leakage, asserted on DOM and focus; (10) all targets ≥44px; (11) fully offline including reload; (12) axe serious/critical = 0 and reduced-motion respected.

**Migration hard guarantee** (S193): the real captured fixture survives byte-for-byte into the default pack, the step is idempotent, and quarantine stays intact.

**Regression net (never break)**: `npm run build` green · persistence · countdown · calendar CRUD · sync convergence · a11y serious=0 · PWA/offline · guest gate · no console errors. Run the fast pack per slice, and the full net plus the iPhone projects at every main commit and pre-deploy. Visual re-baselines happen only at declared design freezes, and the budget is exactly one, at S177.

**Real-iPhone field-test script** (6 steps: A2HS → Dynamic Island → Travel Mode walkthrough → airplane-mode relaunch → offline edit sync → push receipt) lands in `docs/` during Phase 2 and is run twice: after the Nov 9 ship, and at the ~Dec 1 go/no-go.

**Backend**: mocked at the network boundary in the continuous net, with the live smoke test manual-only because of quota. Degraded modes are the acceptance, and every verification pass greps the built `out/` for key material.

## 5. Phase 5 addendum — 2026 UI/UX trend fold-in (approved 2026-07-16, expanded 2026-07-15)

Sourced from three passes of 2026 UI/UX and travel-app trend research, checked against DECISIONS.md before approval. These slot into Phase 5 (S203–S213) when the work is scoped; the IDs below are placeholders pending final numbering.

**Scope notes on already-planned slices (no new slice IDs):**

| Host slice | Scope note |
|---|---|
| **S179** (VT wrapper) | Shared-element view transitions. Name key elements (a destination card's photo and title → its detail page) via `view-transition-name` so `document.startViewTransition()` morphs them instead of hard-cutting. Additive to the existing helper, no interface change. |
| **S178** (Glass 2.0) | A time-of-day / trip-phase ambient background gradient (Himalayan warm ↔ Japan neon, per the existing palette split) that shifts as the countdown crosses trip phases. Pure CSS custom-property animation, token-level, under the same reduced-motion contract as the rest of S178. |

| Slice | Goal / DoD | Hardness |
|---|---|---|
| **S214-candidate** | **Scroll-reveal extension.** Extend S180's `route-accent-engine.tsx` scroll-driven pattern to additional content routes (the Nepal/Japan guides, the photography guide), `@supports`-guarded, with a Firefox static fallback and a reduced-motion audit. No new dependency. | Easy. A mechanical extension of a proven host pattern |
| **S215-candidate** | **3D tilt.** Pointer- and gyro-driven card tilt (framer spring, LazyMotion strict, the same motion platform as S185's hero flip) on destination and recommendation cards. The reduced-motion = NONE hard guarantee (D-007/D-056) disables tilt entirely rather than damping it. Desktop pointer plus mobile deviceorientation, permission-gated on iOS. Scope fence: cards only, one component family. This is the one candidate with no existing host slice, so watch for scope creep (per-breakpoint tuning, hover-vs-touch edge cases). | Hard. No existing host slice, plus novel iOS motion-permission gating, hover-vs-touch divergence, a hard reduced-motion disable to get right, and real scope-creep risk |
| **S216-candidate** | **Weather-aware itinerary tags.** Recommendation and itinerary cards (S203's redesign) pick up a contextual tag such as "good for today's weather", sourced from the existing Open-Meteo cache already wired for S188's Essentials block. Pure client-side derivation, no new API surface. | Easy. Pure derivation over an already-wired cache, with no new dependency or state machine |
| **S217-candidate** | **Critical-docs checklist as a dedicated page**, not an S206 extension, per the owner's explicit ask for a separate page. A new route (naming decided when the work is scoped), checkbox UI over a fixed critical-docs item set (passport, visa, insurance, tickets and so on), gateway-persisted via a new `STORAGE_KEYS` entry (D-097) and **synced**, riding the same tombstone-replace/`stampSync*`/`commit`/outbox pattern S174 builds for expenses in Phase 0 (FU-37). This is confirmed not a Vault-scope change: D-098 governs the Vault's export/import schema only, and D-156 already set this precedent for expenses. The guest stance is asserted explicitly. Depends on S174 landing first, which the P0 → P5 ordering already satisfies; point at S174's landed sync usage as the reference. DoD: the route serves, checkbox CRUD persists and survives reload, the sync convergence spec is green (a two-client check-off race does not get clobbered), and axe is clean. | Hard. A new persisted and synced data domain, not a plain checklist. Harder than S206, it has to prove concurrent check-off convergence, and it has a real dependency on S174 |
| **S218-candidate** | **Small-polish bundle.** Three independent one-file-scale additions, bundled because each is a few lines. (1) Native haptics on key taps (checklist check-off, countdown milestones, save actions) via `navigator.vibrate()`, mobile-only and feature-detected, no dependency. (2) Skeleton/shimmer loading states for lazy-loaded islands (map, journal photos) replacing blank flashes, standard CSS `@keyframes` covered by the existing global `prefers-reduced-motion` block (D-007/D-056a). Verify when scoping that it falls under the auto-neutralized CSS path rather than needing an explicit guard. (3) A micro-celebration on completion (the countdown hits zero, the last checklist item is checked off): a short framer-motion burst using the already-installed dependency, no canvas-confetti. It is triggered imperatively on an event, so it does not fall under `MotionConfig`'s declarative auto-neutralization and needs an explicit `useReducedMotion()` guard per D-056(b). DoD: all three feature-detected or guarded, a reduced-motion audit for (2) and (3), no new dependency. | Moderate. Correctly one bundled slice. Haptics and skeletons are near-one-liners; the micro-celebration's explicit reduced-motion guard is what keeps this off Easy |
| **S219-candidate** | **Trip Wrapped / recap page (capstone).** A shareable end-of-trip summary (days traveled, journal entry count, checklist completion %, cities and legs visited) derived entirely from existing localStorage data: journal (S208), checklist (S217-candidate), itinerary and trip dates (`core/dates`). Read-only derivation, no new API, no new dependency. It uses `navigator.share()` (the Web Share API, native and already free/client-side) with a styled HTML summary card as the fallback and share payload. Confirmed to have no D-002 conflict (pure read, no new storage write) and to touch no LOCKED decision. **Sequencing: it has to land after S208 (journal) and S217-candidate (checklist); both are hard data dependencies.** DoD: the recap page renders correct derived stats against a seeded vault, the share sheet invokes on supporting browsers with a graceful copy-to-clipboard fallback where `navigator.share()` is unavailable, axe is clean, and any reveal animation respects reduced-motion. | Moderate. The slice's own code is contained (read-only derivation, no new API), but there is real sequencing risk since it hard-depends on both S208 and S217-candidate landing first |

**Confirmed no-change** (already on-trend as scoped): motion-as-UX-logic philosophy (S179/S180/S211), the bento grid (S205), Glass 2.0 (S178).

**Explicitly excluded**: AR navigation, in-app translation and native booking, all native-only or paid-API and in conflict with the free/card-free/client-side/no-native scope fences in section 0. iOS Live Activities and widgets, still native-only per 2026 sources, which PWAs cannot touch, consistent with the no-native fence. Social sharing and a community feed, which need a backend and multi-user accounts and do not fit this two-person personal trip app.

**Assessment.** The Phase 5 addendum is six slices (S214–S219-candidate) plus two scope notes riding already-planned hosts (S179, S178). No new subsystems, no new dependencies anywhere in the set, and every reduced-motion-relevant item is checked against D-007/D-056 rather than assumed clean. S218-candidate is genuinely small (three feature-detected one-liners) and is bundled correctly; splitting it into three slices would be overhead for work with no shared review risk. S219-candidate (Trip Wrapped) is the one item with a real dependency edge (S208 and S217-candidate have to land first) and is sequenced accordingly at the tail of Phase 5. It is also the right note to end the milestone on: this is the last major build before maintenance mode, and a read-only, zero-dependency, native-Web-Share capstone that closes out the trip's own data is a fitting bookend rather than scope creep. Sequence Phase 5 as S203 → … → S208 → … → S217-candidate → S218-candidate → S219-candidate, and hold S219-candidate until both its dependencies are done.

## 6. Fourth research-pass fold-in (2026-07-15) — Travel Mode UX deep dive · iPhone 15 Pro specifics · 2026 feature-gap sweep

A cross-check of the fourth, more targeted research pass (memos on travel-companion-mode competitor UX, iPhone 15 Pro mobile-UX specifics, and a 2026 feature-gap sweep pre-filtered against the section-0 fences) against those fences, the section-2 locked decisions, the S171–S219 ledger, and the existing section-5 addendum. Verified in-repo before drafting: `app/layout.tsx` already sets `appleWebApp.statusBarStyle:'black-translucent'`, so Memo 2's status-bar-meta check is already satisfied and needs no action, and it already sets `viewportFit:'cover'` (fact 3). `scripts/gen-sw.mjs`'s hand-maintained `buildManifest()` (fact 1) currently emits no `shortcuts` or `share_target` keys, confirming those are genuinely additive rather than duplicates.

**Scope notes on already-planned slices (no new slice IDs):**

| Host slice | Scope note |
|---|---|
| **S185** (Now/Next hero card) | Three Flighty-inspired refinements to the same card, no interface change. (1) A thin inline elapsed/remaining progress bar under the current activity, pure CSS/JS, works offline. (2) Tap-to-expand: default compact (title, time, progress), tap reveals full details (address, confirmation number, notes) inline instead of navigating away. (3) A manual "recalculate" tap target for when the app was backgrounded and the phase state looks stale. Not true pull-to-refresh, just a deliberate re-evaluation of `nextUp()`. |
| **S184** (`/travel` route bundle) | Four iPhone-15-Pro-specific hardening items on the same safe-area and chrome-free layout work already in scope, each a few CSS lines. (1) A hardcoded ~20px min top-padding fallback alongside `env(safe-area-inset-top)`, for the landscape edge case where the env() value can resolve to 0 even with `viewport-fit=cover` set. (2) An explicit thumb-zone rule pinning Travel Mode's primary actions to a fixed bottom band ≥44×44px inside `env(safe-area-inset-bottom)`, per iPhone 15 Pro Max one-handed-reach data. No rule currently pins primary actions to a band, so this closes that gap. (3) `overscroll-behavior-y: contain` on the Travel Mode root as scoped best-effort bounce mitigation, documented explicitly as a partial fix and a known limitation (current iOS Safari has no full rubber-band kill) so the slice is not failed over residual overscroll. (4) A one-line code comment noting that Dynamic Island models report ~59-62pt top and side safe-area insets in landscape versus ~44-50pt for plain-notch models. Informational, no logic change. |
| **S202** (Pulse cron sender) | The Badging API tie-in: set and clear the installed app icon's unread-count badge (`navigator.setAppBadge`/`clearAppBadge`) from the same SW `push`/`notificationclick` handlers this slice already merges into the gen-sw output. Free, native, and complementary to the already-planned push rather than duplicating it. The dormant-unless-configured gate (the D-038/D-055 pattern) covers this too, so there is no badge without an active push subscription. |

**New candidate slices** (genuinely new capability, no existing host, all passing the section-0 free/card-free/client-side/no-native/no-i18n fences):

| Slice | Goal / DoD | Hardness |
|---|---|---|
| **S220-candidate** | **Share-target inbox.** Register the PWA as an OS share target (a `share_target` manifest entry, hand-added to `gen-sw.mjs`'s `buildManifest()` per fact 1's pattern) so either traveler can hit "Share" on a restaurant, hotel or social post from any other app and land it in a small triage inbox tied to a trip day. Gateway-persisted (a new `STORAGE_KEYS` entry, D-097), local-only: no sync requirement in v1, flagged as a candidate follow-up rather than scope creep here. Pure manifest plus a client-side receive handler, no backend, no new dependency. DoD: the manifest declares `share_target`, shared content lands in the inbox UI, it persists and survives reload, and axe is clean. | Hard. The most structurally novel of the four section-6 candidates, and the only one introducing a brand-new persisted data shape. It also carries real lifecycle decisions (pruning? default day association?) that have to be settled up front |
| **S221-candidate** | **Manifest shortcuts.** Add a `shortcuts` array to the manifest (the same hand-maintained `buildManifest()`, fact 1) so long-pressing the installed PWA icon offers direct jumps, for example "Today's Itinerary", "Add Expense", "Countdown". Zero-dependency and manifest-only; every shortcut target is an existing route. DoD: shortcuts appear on a real installed iOS/Android PWA, each target route resolves correctly, and `pwa.spec` is extended to assert the manifest shape. | Trivial. A pure manifest-only addition, every target already an existing route, no new state |
| **S222-candidate** | **Auto destination-context switch via Geolocation.** Detect which country or city the device is physically in (`navigator.geolocation`, a coarse check against the two legs' known coordinate ranges) and auto-switch the active destination panel (Nepal vs Japan) instead of requiring the manual toggle. Falls back silently and gracefully to the manual toggle if permission is denied or geolocation is unavailable; it never blocks or nags. Native browser API, free, no new dependency. DoD: auto-switch fires on a real or mocked geolocation change, the manual toggle still works and always wins if the user overrides, the permission-denied path leaves current manual behavior fully intact, and axe is clean. | Hard. It reaches outside Travel Mode's own footprint into app-wide behavior. There is no existing geolocation-switch logic to extend, and the permission-denied / manual-always-wins fallback contract is easy to get subtly wrong |
| **S223-candidate** | **Voice-to-text quick capture.** A mic button on the existing journal / day-recap entry field using the native Web Speech API for hands-free dictation. Feature-detected, with the button hidden entirely where unsupported. Free, and it extends an already-shipped feature (the S159/S160 photo and mood pipeline) without touching its stored schema. DoD: dictated text lands in the same entry field a typed one would, feature detection hides the affordance cleanly on unsupported browsers, no new dependency, axe clean. | Easy. Extends an already-shipped feature, feature-detected, no schema touch, no new dependency |

_S215, S217, S220 and S222 are the highest-risk of this set: each either has no existing host pattern to clone, introduces a new persisted or synced data shape, or crosses a scope boundary already flagged as risky. The rest are mechanical extensions of an already-proven pattern, or manifest and one-liner changes. S217-candidate and S219-candidate both sit on the same sync/data-dependency chain (S174 → S217-candidate → S219-candidate, plus S208), so sequence them tightly: a slip in S174 or S208 cascades into both._

**Confirmed no-change**: back/forward gesture nav in standalone mode is moot, because Travel Mode is single-screen per TM-9's zero-chrome-leakage acceptance, so Memo 2's "needs its own in-app back affordance" concern does not apply. 44×44px minimum tap targets are already TM-10's acceptance item. Memo 2's perceived-speed skeleton-screen framing is numeric backing for the already-planned S218-candidate skeleton item, not a new requirement. `apple-mobile-web-app-status-bar-style` is already set correctly in `app/layout.tsx`, verified above.

**Explicitly excluded**: none of the memos' new ideas fail a section-0 scope fence outright. Memo 3's own pre-filtering already rejected AI-generated itineraries, booking and payment, NFC, AR/VR, Siri/Alexa, native Wallet boarding passes, and items duplicating already-shipped or already-planned features, and that filtering checks out against section 0 and section 5's existing exclusions.

**Assessment.** Four new candidate slices (S220–S223-candidate), three scope notes riding existing hosts (S185, S184, S202), and zero rejections against the fences, since Memo 3's own pre-filtering did the fence-checking first. The Memo-1 and Memo-2 pattern held as expected: competitor-UX and iPhone-specifics research mostly refines cards and layout already in scope (S185's Now/Next card, S184's safe-area work), so it rides those slices' DoD rather than minting new IDs, consistent with the S178/S179 precedent in section 5.

Two items are worth flagging as genuinely debatable rather than slam-dunks. **S222-candidate** (geolocation auto-switch) is the one item that reaches slightly outside Travel Mode's own footprint: it changes when the app auto-switches destination context app-wide, not just inside `/travel`. It is smaller and more self-contained than it first appears only if the fallback-to-manual path is airtight, so size it as a genuine nice-to-have rather than assuming it is as contained as S220/S221/S223. **S220-candidate** (share-target inbox) is the most structurally novel of the four, and the only one introducing a new persisted data shape (a triage inbox) rather than a read-derived view or a manifest tweak, so it carries more review weight than its one-line manifest-registration description suggests. Flag that when scoping, so the inbox's storage and lifecycle get sized (does shared content ever get pruned? tied to which trip day by default?) rather than being treated as manifest-only work.

All four candidates and all three scope notes stay zero-new-dependency and zero-backend, consistent with the rest of Phase 5. Slot S220–S223-candidate into Phase 5's sequencing alongside S214–S219-candidate (there is no fixed order dependency among them, unlike S219-candidate's hard S208/S217 dependency), and fold the three scope notes into S185/S184/S202 when those slices come up, not before.

## 7. Final-sweep fold-in (2026-07-16) — broader competitor sweep · Core Web Vitals audit · 2026 web-platform capability check · gap analysis

A cross-check of the final-sweep research pass (four memos, specifically hunting for anything still missing before the plan locks) against the section-0 scope fences, the section-2 locked decisions, and the full S171–S223-candidate ledger. This is the last research fold-in before build starts. Every memo's own pre-filtering had already done real work (Memo A explicitly rejected email-import, price-tracking and vision-API landmark ID as scope-fence failures or redundant), and nothing below collides with an existing slice ID or a LOCKED decision.

**Scope notes on already-planned slices (no new slice IDs):**

| Host slice | Scope note |
|---|---|
| **S188** (Essentials block) | Two additions riding the same component. (1) Memo A's transit-leg deep-link cards: extend the already-planned next-flight deep-link pattern to inter-city transfer days (for example "Kathmandu → Osaka"), deep-linking to Rome2Rio, Google Flights or Maps transit for that specific route. Free, client-side, the same shape as the flight deep-link already in S188's DoD. (2) Memo C's Screen Wake Lock: acquire `navigator.wakeLock` while the Essentials/agenda view is actively on-screen (transit days, day-of itinerary) and release it on blur or navigation. iOS Safari 18.4+, feature-detected no-op elsewhere, no dependency. |
| **Safety-kit content** (the pre-v5 safety/documents page) | Memo D's power and data-budget awareness copy (teahouse charging costs, Everest Link data pricing) is a content addition to the already-shipped safety-kit page, not a slice. Near-zero effort, no code path. |
| **S217-candidate** (critical-docs checklist page) | Memo D's pre-departure readiness check (a "day-zero" checklist: offline maps downloaded, power bank charged, docs synced, SIM/eSIM sorted) rides as a second section or tab on S217-candidate's page rather than minting a fourth checklist-shaped route (packing is S206, critical-docs is S217-candidate). Same gateway-persisted and synced pattern, same page, different item set. See the assessment below for why this is a scope note rather than a new slice. |

**New candidate slices** (genuinely new capability, no existing host, checked against section 0's free/card-free/client-side/no-native fences):

| Slice | Goal / DoD | Hardness | Notes |
|---|---|---|---|
| **S224-candidate** | **Map-linked itinerary editing.** Let the map (currently a polished mock per D-003/D-079, separate from the itinerary planner) become an input to day-planning: drag a pin onto "Day 3", with client-side haversine distance ordering for that day's stops and no paid routing API. DoD: drag-to-assign works on desktop and touch, the day's itinerary items re-order by haversine distance from the dropped pin, it persists via the existing gateway CRUD, axe is clean, and there is no new dependency. | Hard. The only candidate merging two previously separate subsystems (map and itinerary planner) into one interaction. It needs real UX-design judgment on the drag affordance and its touch equivalent, and there is no existing host slice | |
| **S225-candidate** | **Responsive image pipeline.** AVIF with a WebP fallback via `<picture>` (a build-time script, static-export compatible since `next/image`'s default loader needs a server, consistent with D-010), responsive `srcset`/`sizes` so mobile does not download desktop-res images, and `fetchpriority="high"` with no lazy-load on the LCP hero image specifically (everything else stays lazy). DoD: the build script emits AVIF/WebP variants plus srcset for the existing destination photography, Lighthouse/CWV LCP measurably improves on a representative page, and there is no regression to the existing alt-text and a11y contract. | ~~Hard~~ **Moderate** (revised) | Verified in-repo: `scripts/gen-images.mjs` (already shipped, S51/M12) already does AVIF and WebP via `sharp`, plus LQIP and `image-manifest.json`, and `components/optimized-image.tsx` already renders `<picture>` with `fetchpriority="high"` on priority images and `loading="lazy"` elsewhere. Most of this DoD is already shipped. The one real gap is multi-width `srcset`/`sizes`; today's pipeline emits one resolution per source. This extends existing tooling rather than building it, hence the downgrade. |
| **S226-candidate** | **Self-hosted subsetted web fonts.** Self-hosted subsetted WOFF2 (an 80-93% typical size cut versus current font loading), a variable font if 3 or more weights are in use, `font-display: swap`, and `<link rel="preload" as="font">` for the one above-the-fold critical face. DoD: fonts self-hosted and subsetted, preload and swap verified in the built output, no visual regression (font metrics match, no layout shift introduced), CLS unaffected. | ~~Moderate~~ **Easy** (revised) | Verified in-repo: `app/layout.tsx` already uses `next/font/google`, which already self-hosts at build time (zero runtime Google requests), already subsets (`subsets:['latin']`), already defaults to `font-display:swap`, and already auto-preloads root-layout fonts. Nearly the entire DoD is already default `next/font` behavior, hence the downgrade. |
| **S227-candidate** | **Cross-tab write coordination via the Web Locks API.** Wrap writes through `core/storage/gateway.ts` (the D-097 LOCKED single key registry) with `navigator.locks.request()` so opening the app in two tabs cannot race and corrupt itinerary, expense or checklist edits. iOS Safari 15.4+, purely additive (no key-shape or interface change), with the D-097 never-throw contract preserved. DoD: a two-tab concurrent-write spec shows no corruption with the lock and reproducible corruption without it, the existing gateway unit suite is unchanged, and there is a feature-detected no-op fallback where unsupported. | ~~Moderate~~ **Hard** (revised) | Verified in-repo: `gateway.ts`'s primitives are fully synchronous, but `navigator.locks.request()` is unavoidably Promise-based. Wrapping writes in it moves every caller's call signature from sync `void` to async, a real breaking ripple across every gateway-backed store, and not the "purely additive, zero interface change" this slice's own text claims. A lock-acquisition failure is also a brand-new async failure mode that has to be swallowed correctly to preserve D-097's never-throw contract. Call out the sync→async ripple explicitly rather than selling this as additive. |
| **S228-candidate** | **Compressed export/import via the Compression Streams API.** Gzip the JSON export/import blobs (Vault export/import per D-098, S174's expenses backup-restore, and S217-candidate's checklist export if it ships one) client-side via `CompressionStream`/`DecompressionStream`, with zero library weight. iOS Safari 16.4+, with a feature-detected fallback to uncompressed for unsupported browsers. DoD: the exported file is gzip-compressed and measurably smaller, it round-trips correctly through import on both a supporting and a non-supporting browser path, and there is no schema change to D-098's export/import contract. | Easy (confirmed) | Verified in-repo: `core/vault/export-import.ts`'s `exportItinerary()`/`importItinerary()` are a clean plain-string boundary with no schema entanglement, so Easy holds. |
| **S229-candidate** | **Sync-status affordance.** A small badge or toast surfacing "N changes pending sync" and "last synced Xh ago", reading the already-existing sync state (the `stampSync*`/outbox pattern S174 builds for expenses, extended by S217-candidate for the checklist, plus the itinerary's existing D-038/D-156 sync). Read-only UI over existing state, not a new sync mechanism. DoD: the badge reflects the real pending-change count and last-sync timestamp across the itinerary, expenses and checklist domains, it updates live on reconnect, axe is clean, and there is no new storage write. | Moderate (confirmed, for a different reason) | Verified in-repo: `core/sync/outbox.ts` already unifies all three domains under one `OutboxSlot` shape with a clean `outboxDirty(domain)` read, so aggregating the pending count is low-risk, and not three separately-evolved shapes as first described. The real new work is the "last synced" half: grepping the tree for `lastSync`/`syncedAt` returns zero hits, so a last-ack timestamp has to be added to `outbox.ts` from scratch, and the badge has to tolerate a fourth (checklist) domain landing later. Moderate holds, for a different reason than originally stated. |
| **S230-candidate** | ~~**Live location share between travelers**~~ — **out of scope for v5 (decided 2026-07-16): not implemented as part of this milestone, held on the backlog indefinitely, not pending sign-off.** The concept was to piggyback on S222-candidate's `navigator.geolocation` dependency and write a last-known GPS ping to Firestore so each traveler could see the other's last-known position on separation-risk trek days. It would have been the first feature writing another person's real-time-ish personal location data to the shared Firestore tree, a materially different privacy and consent surface than anything else in the plan. No work will be scoped and no Firestore rules touched for this v5 cycle. The row is kept for the historical record only; reopen it only on a future explicit ask, not by inference. | N/A. Dropped from v5 | |

**Confirmed no-change**: GitHub Pages cache-control (Memo B) is moot, since the SW already fills that role and content-hashed filenames are already handled by Next.js. File System Access, Contact Picker, WebOTP, Idle Detection, Background/Periodic Sync, Storage Buckets and Web NFC (Memo C) are either unimplemented in WebKit or have no real iOS-PWA use case here, and Memo C correctly surveyed and passed over them itself.

**Explicitly excluded**: email-forward auto-import (Memo A) needs a backend beyond the one Worker and fails the one-Worker / no-new-backend-surface fence. Price-tracking (Memo A) needs paid monitoring infrastructure and fails the free/card-free fence. Vision-API landmark ID (Memo A) is redundant with the already-planned S199 chat concierge, and there is no net-new capability for the review cost of a second AI surface.

**Risk flags on already-planned items (Memo D):**

- **The AI chat concierge (S197-S199)** is called out as the highest-risk, lowest-necessity item in the plan. Checked against the cut order in section 0, the concierge is already the second thing dropped under pressure (push, then concierge, then Trip Packs UI, then trim the Phase-5 sweep), one slot above only "trim the sweep", and S197 is explicitly a feasibility spike with a per-feature go/no-go built in before any Worker code is written. The plan already treats this as cautiously as Memo D wants, so no devplan change is needed. Use S197's go/no-go checkpoint to re-confirm explicitly that the concierge is worth the backend and auth surface once real Gemini and Groq quota numbers are in hand, rather than assuming approval carries forward automatically from the original greenlight.
- **S222-candidate (geolocation auto-switch)**: agreed with Memo D. This was already flagged in section 6 as reaching outside Travel Mode's own footprint and sized as a nice-to-have, not a core item. A date-based default already covers the overwhelming majority of the value at zero GPS, battery and accuracy cost, since this trip has fixed, known per-destination dates. Treat S222-candidate as the first thing cut from the S220-223 candidate set if Phase 5 runway is tight. No devplan change required, just a sequencing and priority note.
- **S223-candidate (voice-to-text journal)**: agreed that the marginal value is low, since OS-keyboard dictation already gives free-text fields this for free, but it does not deserve urgent cutting. It is already scored Easy, feature-detected, touches no schema, and extends an already-shipped pipeline, so the engineering cost of keeping it is close to zero and there is little runway to be gained by cutting it specifically. Deprioritize it (build it last among S220-223, if at all) rather than cutting it outright.

**Assessment.** Seven new candidate slices (S224–S230-candidate) and three scope notes (S188 ×2, S217-candidate, safety-kit content), with no collisions against any existing slice ID and no LOCKED-decision conflicts. One judgment call is worth flagging: Memo D's pre-departure readiness checklist is a scope note riding S217-candidate rather than a new slice, because a fourth near-identical gateway-persisted-checkbox page (after packing at S206 and critical-docs at S217-candidate) is proliferation, not new capability. Folding it into S217-candidate's page as a second section reuses the exact same sync and persistence work already being built there, and keeps the checklist surface to two pages instead of three. S230-candidate (live location share) needed an explicit decision before any work was scoped, and the answer (2026-07-16) is that it is out of scope for v5, held indefinitely, not a pending item. Everything else in this final sweep (S224-S229) is properly scoped and zero-new-dependency, except S225 and S226's build-tooling additions, which are the right kind of new: build-time rather than runtime, and free. Codebase verification found both smaller in practice than first scoped. All of it slots into Phase 5 alongside S214-S223 with no fixed ordering dependency among them.

**Decision (2026-07-16), binding on S190 and Travel Mode entry:** a persistent Travel Mode entry button in the top-right of the app-wide nav chrome, available on every page and not gated by the trip-date window, so Travel Mode is explorable and previewable any time before Dec 9 once it ships. The exit affordance is a hard DoD item on S190, not optional polish. Folded into the charter in section 0, the locked-decisions table in section 2, and S190's own row above.

**The plan is locked as of 2026-07-16.** Four independent final-sweep passes, on top of two prior addenda, surfaced no structural gaps, no scope-fence violations, and nothing that blocks or reorders the Phase 0-4 core work. Every genuinely new idea across all three addenda (sections 5, 6 and 7) is either a small scope note on an already-planned host or a self-contained, appropriately-sized Phase-5 candidate slice. Final decisions of 2026-07-16: S230-candidate (live location share) is out of v5 entirely, dropped rather than deferred, so Phase 5's candidate pool is S214-S229; and Travel Mode's entry point is amended (sections 0 and 2, and S190) to a persistent top-right nav-chrome button, explorable pre-trip, with a hard exit-affordance requirement. Nothing else in sections 0-6 or the core phase structure changes. The two risk flags worth a fresh look before Phase 4/5 execution (re-confirming the concierge at S197's go/no-go, and deprioritizing S222-candidate) remain sequencing notes, not devplan edits.
